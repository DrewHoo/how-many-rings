node_modules
dist
.vite
.DS_Store
*.log
*.tsbuildinfo

# Generated by the data-fetch step (scripts/fetch-*). Regenerated by CI on every
# deploy — kept out of git so the repo doesn't bloat as datasets grow.
public/data/

# Per-entity fetch cache (see references/data-sourcing.md)
scripts/.cache/
